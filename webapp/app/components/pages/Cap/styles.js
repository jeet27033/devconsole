import styled, { css } from 'styled-components';

export const spinnerStyle = css`
  &.ant-spin.ant-spin-spinning.cap-container.spinner {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }
`;
