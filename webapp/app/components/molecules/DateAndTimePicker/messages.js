import { defineMessages } from 'react-intl';

export const scope = 'devconsole.components.molecules.DateAndTimePicker';

export default defineMessages({
  apply: {
    id: `${scope}.apply`,
    defaultMessage: 'Apply',
  },
  cancel: {
    id: `${scope}.cancel`,
    defaultMessage: 'Cancel',
  },
  customRange: {
    id: `${scope}.customRange`,
    defaultMessage: 'Custom Range',
  },
});
